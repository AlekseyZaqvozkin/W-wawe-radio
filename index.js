function auto_grow(el) {
    // if (el.scrollHeight > el.clientHeight) 
    	// el.style.height = el.scrollHeight + 'px'
        
            el.style.height = "5px";
            el.style.height = (el.scrollHeight) + "px";
         
}
